packages:
  rootfs:
  - system/cos-config
  - system/tumbleweed
  - utils/installer
  - kernel/vanilla
  uefi:
  - live/systemd-boot
  - live/boot
  isoimage:
  - live/syslinux
  - live/boot
  # Really simple/dumb initramfs until we go into dracut
  initramfs:
  - live/minimal
  - kernel/vanilla-minimal
  - live/init
  - live/boot

overlay: "true"
image_prefix: "cOS-0."
image_date: "true"

luet:
  config: build/conf.yaml
