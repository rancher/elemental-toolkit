name: "minimal"
category: "live"
version: "0.1+2"

hidden: true # No need to make it installable for now, except for initramfs
# TODO: If we are going with dracut, this will become deprecated
description: "tumbleweed-minimal used in initramfs"
zypper_packages: >-
    util-linux
    coreutils
    findutils
    kmod